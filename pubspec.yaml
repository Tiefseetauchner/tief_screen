name: tief_screen
description: Screenshot taking and management library for Flutter integration tests.
version: 0.1.1
repository: https://github.com/Tiefseetauchner/tief_screen

environment:
  sdk: ^3.12.2
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
